namespace ScriptsBank.Time;

public class TimeSpan
{
    public TimeSpan()
    {
        // var txtTime = _currentTime.Hours > 0 ? $"{_currentTime.Hours}h {_currentTime.Minutes}'" 
        //     : $"{_currentTime.Minutes}' {_currentTime.Seconds}''";
        // var txtTime = _currentTime.Hours > 0 ? _currentTime.ToString(@"hh\:mm")
        //     : _currentTime.ToString(@"mm\:ss");   
    }
}